# Core (v2 ATS-registry pipeline)
requests>=2.28        # ATS JSON APIs (Greenhouse/Lever/Ashby/Workday) + Serper
litellm>=1.40         # one interface to OpenAI/Anthropic/Gemini/DeepSeek/Groq/... (LLM eval)
PyYAML>=6.0           # read config/*.yaml + the company registry
python-dotenv>=1.0    # load .env

# v7 — PRIMARY discovery volume: Indeed/Naukri/LinkedIn/Google Jobs/Glassdoor/
# ZipRecruiter/Bayt/BDJobs via one uniform API. Without this, discovery is limited
# to the ATS registry + guest-scrape sources (see README's Honest limits).
python-jobspy>=1.1

# Used by the (deferred) freelance mode's older scrape path
pydantic>=2

# Optional — only for the Tier-3 `serper` fallback (crawling a company's own
# careers domain) and the legacy freelance scrape. Not needed for Tier-1/2 ATS.
# crawl4ai>=0.4

# Optional — only to read a PDF résumé in `python job/init_profile.py resume.pdf`
# pypdf>=4.0
