venv/
__pycache__/
*.pyc
.env
output/
.DS_Store
*.pdf
*.docx
*.txt
!requirements.txt

# Generated run artifacts — reports/raw dumps/dedup caches from actual or dry runs,
# not source. companies_cursor.json (v10 unique-rotation state) is also run state.
job/reports_*/
job/seen_fp*.json
job/companies_cursor.json

# Personal profile (v11) — generated by job/init_profile.py from your résumé;
# config/profile.yaml.example is the tracked template, this is your own data.
config/profile.yaml
